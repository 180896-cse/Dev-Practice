// Given an array and a value, returns the index of the last occurence of the value. If the value is not found, returns -1.

let arr = [10, 20, 30, 20];
let key = 20;


lastIndexOf = (arr, key) => {
       return arr.lastIndexOf(key);
}

console.log(lastIndexOf(arr,key));
